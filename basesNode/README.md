# Notas:

Esta es mi primer aplicación de consola en Node.

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base de la tabla de multiplicar.     [número] [requerido]
  -l, --list     Muestra en pantalla la tabla de multiplicar
                                                     [booleano] [defecto: false]
  -h, --hasta    Es el tamaño o el número hasta el que se va a multiplicar la
                 base.                                      [número] [requerido]
``` 
